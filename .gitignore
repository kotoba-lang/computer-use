.cpcache/
.nrepl-port
target/
.DS_Store
# CUA bot artefacts: screenshots and receipts are session output, not source.
# Captures normally live under CUA_TMPDIR (default /tmp); these patterns keep
# them out of the tree if that is ever pointed at the checkout.
cua-*.png
cua-*.bmp
cua-*.swift
receipts/
.cua/
